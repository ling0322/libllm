[package]
name = "llm-capi"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "The C interface to the llm crate, as a shared library"

[lib]
name = "llm_capi"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
llm = { path = "../llm" }
